<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.ulstu_team.ulstuschedule">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ru.ulstu_team.ulstuschedule.ui.old.start.StartActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="ru.ulstu_team.ulstuschedule.ui.old.faculties.FacultiesActivity" />
        <activity
            android:name="ru.ulstu_team.ulstuschedule.ui.old.cathedries.CathedriesActivity"
            android:label="@string/title_activity_cathedries"
            android:theme="@style/AppTheme" />
        <activity android:name="ru.ulstu_team.ulstuschedule.ui.old.groups.GroupsActivity" />
        <activity android:name="ru.ulstu_team.ulstuschedule.ui.old.ScheduleActivity" />
        <activity android:name="ru.ulstu_team.ulstuschedule.ui.old.teachers.TeachersActivity" />

        <service android:name=".data.remote.sync.ScheduleAuthenticatorService">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="78ecd023a340c012d7b3f24601fe58412bfaa4c1" />

    </application>

</manifest>